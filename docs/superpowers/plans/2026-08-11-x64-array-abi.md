# خطة الموجة 6 — ABI الصفيف x64 (TDD)

التصميم: `docs/superpowers/specs/2026-08-11-x64-array-abi-design.md`

- [x] Design doc: `docs/superpowers/specs/2026-08-11-x64-array-abi-design.md`
- [x] RED: `tests/test_x64_array_abi.cpp` — قراءة/كتابة عنصر مصفوفة نصوص
      (SIB ×8 + قيمة 8 بايت)، مصفوفة أعداد (SIB ×8 + قيمة 4 بايت)، فتحة
      مؤشر عبر EBP، مسار CalcArrayOffset — وكلها فاشلة على الكود القديم (8/8)
- [x] GREEN: أوبكود QWORD الجديدة (MOVEAXSIB8/MOVECXEAXOFF8/MOVEAXECX8/MOVEAXOFFECX8)
      في `ASMWriter.h`/`ASMWriter.cpp`
- [x] GREEN: `TaskEmitter.cpp` — فتحات مؤشر الصفيف 8 بايت (MemArr/EbpArr)،
      حارس قيمة النص، سلّم SIB ×8، توجيه 103/203 إلى متغيرات QWORD
- [x] GREEN: `CalcArrayOffset` في `ASMWriter.cpp` — فتحات 8 بايت
- [x] GREEN: `DBDLLCore.cpp` — جدول ref بعناوين 8 بايت في CreateArray وجميع
      مستهلكيه (ExpandArray/ReDimCore/FreeStringsFromArray/ClearDataBlock/
      EmptyArray/ArrayInsert*/ArrayDeleteElement) + نسختا `CMemblocks.cpp`
- [x] تحديث اختبار الموجة 5 المثبَّت → `StringArrayElementAccessUsesX64RefTable` (×8)
- [x] الاختبارات الكاملة + البناء الكامل + مصفوفة الملحقات + ctest
      (962/0/1؛ كل الملحقات تُبنى؛ ctest 100%)
- [x] توثيق الحالة في `docs/17` وتحديث صندوق الخطة
